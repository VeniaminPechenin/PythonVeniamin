def ReplaceSymbol(stringOfWords):
    for i in range(len(stringOfWords)):
        if stringOfWords[i] :
            stringOfWords = stringOfWords[i].replace("(")
        if stringOfWords[i]:
            stringOfWords = stringOfWords[i].replace(")")

string = "qqw"
ReplaceSymbol(string)